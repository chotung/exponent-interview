# Environment Configuration Example
# Copy this file to .env and update with your values

# Application
NODE_ENV=development
PORT=3000

# Database Configuration
# Options: 'sqlite' (development) or 'postgres' (production)
DATABASE_TYPE=sqlite

# SQLite Configuration (for local development)
SQLITE_DB_PATH=./data/credit_card.db

# PostgreSQL Configuration (for production)
# Option 1: Use connection string
# DATABASE_URL=postgresql://user:password@localhost:5432/credit_card_platform

# Option 2: Individual connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=credit_card_platform
DB_USER=credit_card_app
DB_PASSWORD=your_password_here
DB_POOL_MAX=20
DB_SSL=false

# For AWS RDS PostgreSQL in production:
# DB_HOST=your-rds-instance.abc123.us-east-1.rds.amazonaws.com
# DB_SSL=true
# DATABASE_URL=postgresql://user:pass@your-rds-instance.abc123.us-east-1.rds.amazonaws.com:5432/credit_card_platform
